# 🏗️ DOCKER ARCHITECTURE DIAGRAM

## System Overview

```
┌───────────────────────────────────────────────────────────────────────────────────┐
│                              DOCKER COMPOSE ENVIRONMENT                            │
│                                  (blog-network)                                    │
│                                                                                    │
│  ┌─────────────────────────────────────────────────────────────────────────────┐ │
│  │                           INFRASTRUCTURE LAYER                               │ │
│  │                                                                              │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │ │
│  │  │ PostgreSQL16 │  │   Redis 7    │  │    MinIO     │  │   Ollama     │  │ │
│  │  │   :5432      │  │   :6379      │  │ :9000/:9001  │  │   :11434     │  │ │
│  │  │              │  │              │  │              │  │              │  │ │
│  │  │ ┌──────────┐ │  │ ┌──────────┐ │  │ ┌──────────┐ │  │ ┌──────────┐ │  │ │
│  │  │ │pgvector  │ │  │ │AOF Persist│ │  │ │3 Buckets │ │  │ │llama3    │ │  │ │
│  │  │ │16 Tables │ │  │ │BullMQ    │ │  │ │S3-API    │ │  │ │nomic-emb │ │  │ │
│  │  │ └──────────┘ │  │ └──────────┘ │  │ └──────────┘ │  │ └──────────┘ │  │ │
│  │  │              │  │              │  │              │  │              │  │ │
│  │  │   Volume:    │  │   Volume:    │  │   Volume:    │  │   Volume:    │  │ │
│  │  │postgres_data │  │  redis_data  │  │  minio_data  │  │ ollama_data  │  │ │
│  │  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  │ │
│  │         │                 │                 │                 │          │ │
│  └─────────┼─────────────────┼─────────────────┼─────────────────┼──────────┘ │
│            │                 │                 │                 │             │
│            └─────────────────┴─────────────────┴─────────────────┘             │
│                                      │                                         │
│  ┌───────────────────────────────────┴────────────────────────────────────┐   │
│  │                          APPLICATION LAYER                              │   │
│  │                                                                          │   │
│  │  ┌─────────────────────────────────┐  ┌───────────────────────────┐   │   │
│  │  │         API Server              │  │     Video Worker          │   │   │
│  │  │         :3000                   │  │     (Background)          │   │   │
│  │  │                                 │  │                           │   │   │
│  │  │  ┌────────────────────────┐    │  │  ┌────────────────────┐  │   │   │
│  │  │  │ Express/Fastify        │    │  │  │  BullMQ Consumer   │  │   │   │
│  │  │  │ TypeScript             │    │  │  │  FFmpeg Encoder    │  │   │   │
│  │  │  │ JWT Auth               │    │  │  │  4 Qualities       │  │   │   │
│  │  │  │ REST API               │    │  │  │  HLS Output        │  │   │   │
│  │  │  └────────────────────────┘    │  │  └────────────────────┘  │   │   │
│  │  │                                 │  │                           │   │   │
│  │  │  Health: /health                │  │  Temp: /tmp/video-proc   │   │   │
│  │  │  Node.js 20 (non-root)          │  │  Node.js 20 (non-root)   │   │   │
│  │  └─────────────────────────────────┘  └───────────────────────────┘   │   │
│  │                                                                          │   │
│  └──────────────────────────────────────────────────────────────────────────┘   │
│                                                                                    │
│  ┌───────────────────────────────────────────────────────────────────────────┐  │
│  │                      OPTIONAL TOOLS (--profile tools)                      │  │
│  │                                                                            │  │
│  │    ┌─────────────────┐                    ┌─────────────────┐            │  │
│  │    │    pgAdmin      │                    │ Redis Commander │            │  │
│  │    │    :5050        │                    │     :8081       │            │  │
│  │    └─────────────────┘                    └─────────────────┘            │  │
│  │                                                                            │  │
│  └────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                    │
└────────────────────────────────────────────────────────────────────────────────────┘
```

## Data Flow

```
┌──────────┐
│  Client  │
└────┬─────┘
     │ HTTP Request
     ▼
┌─────────────┐
│ API Server  │◄────────┐
│   :3000     │         │
└─────┬───────┘         │ Queue Job
      │                 │
      ├─────────────────┼──────────┐
      │                 │          │
      ▼                 ▼          ▼
┌──────────┐      ┌──────────┐  ┌──────────┐
│PostgreSQL│      │  Redis   │  │  MinIO   │
│ (Query)  │      │ (Cache)  │  │ (Upload) │
└──────────┘      └──────────┘  └────┬─────┘
                        │             │
                        │ Dequeue     │ Process
                        ▼             ▼
                  ┌──────────────────────┐
                  │   Video Worker       │
                  │   (FFmpeg Encoding)  │
                  └──────────────────────┘
                              │
                              ▼
                        ┌──────────┐
                        │  MinIO   │
                        │(HLS Out) │
                        └──────────┘
```

## AI Search Flow

```
┌──────────┐
│  Client  │
│ (Search) │
└────┬─────┘
     │
     ▼
┌─────────────┐
│ API Server  │
└─────┬───────┘
      │
      ├─────────────────┐
      │                 │
      ▼                 ▼
┌──────────┐      ┌──────────┐
│  Ollama  │      │PostgreSQL│
│(Embedding)│      │(pgvector)│
└─────┬────┘      └────┬─────┘
      │                │
      │ Vector         │
      └────────►───────┘
                │
                ▼
           ┌─────────┐
           │ Results │
           │ + Score │
           └─────────┘
```

## Video Processing Pipeline

```
1. Upload Request
   ┌──────────┐
   │  Client  │
   └────┬─────┘
        │ POST /api/videos/upload-url
        ▼
   ┌─────────────┐
   │ API Server  │
   └─────┬───────┘
        │ Generate presigned URL
        ▼
   ┌──────────┐
   │  MinIO   │
   └────┬─────┘
        │ Return URL
        ▼
   ┌──────────┐
   │  Client  │
   └────┬─────┘
        │ Direct upload
        ▼
   ┌──────────┐
   │  MinIO   │
   │ (Raw)    │
   └──────────┘

2. Processing
   ┌──────────┐
   │  Client  │
   └────┬─────┘
        │ POST /api/videos/:id/confirm
        ▼
   ┌─────────────┐
   │ API Server  │
   └─────┬───────┘
        │ Queue encoding job
        ▼
   ┌──────────┐
   │  Redis   │
   │(BullMQ)  │
   └────┬─────┘
        │ Dequeue
        ▼
   ┌──────────────────────┐
   │   Video Worker       │
   │   1. Download raw    │
   │   2. Extract metadata│
   │   3. Gen thumbnail   │
   │   4. Encode HLS      │
   │   5. Upload chunks   │
   │   6. Delete raw      │
   └──────────┬───────────┘
              │
              ▼
         ┌──────────┐
         │  MinIO   │
         │ (HLS)    │
         └──────────┘

3. Streaming
   ┌──────────┐
   │  Client  │
   │(Player)  │
   └────┬─────┘
        │ GET master.m3u8
        ▼
   ┌──────────┐
   │  MinIO   │
   │(HLS URL) │
   └──────────┘
        │ Adaptive streaming
        ▼
   ┌──────────┐
   │  Client  │
   │(Playing) │
   └──────────┘
```

## Volume Persistence

```
Host Machine
└── Docker Volumes
    ├── blog-postgres-data/
    │   └── /var/lib/postgresql/data
    │       ├── base/
    │       ├── pg_wal/
    │       └── ... (16 tables)
    │
    ├── blog-redis-data/
    │   └── /data
    │       └── appendonly.aof
    │
    ├── blog-minio-data/
    │   └── /data
    │       ├── videos/
    │       ├── images/
    │       └── thumbnails/
    │
    └── blog-ollama-data/
        └── /root/.ollama
            └── models/
```

## Network Isolation

```
┌────────────────────────────────────────┐
│        Docker Network: blog-network    │
│              (bridge mode)              │
│                                         │
│  All services communicate internally   │
│  via service names (DNS resolution)    │
│                                         │
│  Examples:                              │
│  - postgres:5432                        │
│  - redis:6379                           │
│  - minio:9000                           │
│  - ollama:11434                         │
│                                         │
└────────────────────────────────────────┘
                    │
                    │ Only exposed ports
                    │ visible to host
                    ▼
         ┌─────────────────────┐
         │   Host Machine      │
         │   localhost:5432    │
         │   localhost:6379    │
         │   localhost:9000    │
         │   localhost:11434   │
         │   localhost:3000    │
         └─────────────────────┘
```

## Security Layers

```
1. Network Layer
   ┌────────────────────┐
   │  bridge network    │
   │  (isolated)        │
   └────────────────────┘

2. Authentication Layer
   ┌────────────────────┐
   │ PostgreSQL: user+pw│
   │ Redis: password    │
   │ MinIO: access key  │
   │ JWT: secret        │
   └────────────────────┘

3. Container Layer
   ┌────────────────────┐
   │ Non-root users     │
   │ Read-only rootfs   │
   │ Resource limits    │
   └────────────────────┘

4. Application Layer
   ┌────────────────────┐
   │ Input validation   │
   │ Rate limiting      │
   │ CORS policy        │
   └────────────────────┘
```

## Health Check Flow

```
Docker Daemon (every 30s)
      │
      ├─► PostgreSQL: pg_isready
      │        └─► healthy/unhealthy
      │
      ├─► Redis: PING
      │        └─► healthy/unhealthy
      │
      ├─► MinIO: /minio/health/live
      │        └─► healthy/unhealthy
      │
      ├─► Ollama: /api/tags
      │        └─► healthy/unhealthy
      │
      └─► API Server: curl /health
               └─► healthy/unhealthy

Status visible via:
  - docker compose ps
  - make status
```

## Resource Allocation (Recommended)

```
┌──────────────────────────────────────────┐
│           Total: 16GB RAM, 8 Cores       │
├──────────────────────────────────────────┤
│                                          │
│  PostgreSQL:    4GB RAM, 2 Cores         │
│  Redis:         2GB RAM, 1 Core          │
│  MinIO:         2GB RAM, 1 Core          │
│  Ollama:        4GB RAM, 2 Cores (GPU)   │
│  API Server:    2GB RAM, 1 Core          │
│  Video Worker:  2GB RAM, 1 Core          │
│                                          │
└──────────────────────────────────────────┘
```

## Management Tools Access

```
Development Tools:

┌─────────────────────────────────────────┐
│          Web Interfaces                 │
├─────────────────────────────────────────┤
│                                         │
│  MinIO Console                          │
│  └─► http://localhost:9001              │
│      (minio_admin / password)           │
│                                         │
│  pgAdmin (optional)                     │
│  └─► http://localhost:5050              │
│      (admin@blogplatform.local / pw)    │
│                                         │
│  Redis Commander (optional)             │
│  └─► http://localhost:8081              │
│                                         │
└─────────────────────────────────────────┘

Command Line Tools:

┌─────────────────────────────────────────┐
│          Shell Access                   │
├─────────────────────────────────────────┤
│                                         │
│  make shell-api                         │
│  make shell-postgres                    │
│  make shell-redis                       │
│  make shell-worker                      │
│                                         │
└─────────────────────────────────────────┘
```

---

**Legend:**
- `┌─┐ └─┘`: Container boundaries
- `─►`: Data flow direction
- `│`: Connection/relationship
- `:PORT`: Exposed port number
- Volume: Persistent storage
